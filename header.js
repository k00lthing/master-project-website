export function createHeader(element) {
  
    element.insertAdjacentHTML('afterbegin',
    `
    <header>
    <h1>I!&3_OCR</h1>
    <p>typographic experiments on human-only legibility</p>
    </header>
    `)
}
